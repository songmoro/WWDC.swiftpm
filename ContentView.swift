import SwiftUI


struct ContentView: View {
    var body: some View {
        ZStack(){
            Text("asd")
        }
    }
}

struct ContentView_Priview: PreviewProvider {
    static var previews: some View {
            ContentView()
        }
}
